# notebooks/common

Shared helper modules imported by the numbered notebooks (e.g. metrics computation,
config loading, results-file writing) go here once Phase 2 benchmarking code is
written. Empty for now — placeholder only.
